<UserControl x:Class="dayouAWSWh2.UC.ucHostOutTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:dayouAWSWh2.UC"
             mc:Ignorable="d" 
             d:DesignHeight="995" d:DesignWidth="1720">
    <Grid>
        <TabControl>
            <TabItem Header="SW 의장 출고내역">
                <local:ucHostOut x:Name="ucHostOut" Grid.Row="0" />
            </TabItem>

            <TabItem Header="SP3 의장 출고내역">
                <local:ucHostOutSP3 x:Name="ucHostOutSP3" Grid.Row="0" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
