<UserControl x:Class="dayouAWSWh2.UC.ucHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:dayouAWSWh2.UC"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="995" d:DesignWidth="1720" KeyDown="UserControl_KeyDown">
    <UserControl.Resources>
        <Style x:Key="BoldCellStyle" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        
        <Style x:Key="RedTextIfLackStyle" TargetType="TextBlock" BasedOn="{StaticResource DataGridCellCenter}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding STOCK_FR}" Value="부족">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RedTextIfLackStyleR2" TargetType="TextBlock" BasedOn="{StaticResource DataGridCellCenter}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding STOCK_R2}" Value="부족">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RedTextIfLackStyleR3" TargetType="TextBlock" BasedOn="{StaticResource DataGridCellCenter}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding STOCK_R3}" Value="부족">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border Padding="8">
            <Grid Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <local:ucToolbarBtn x:Name="ucToolbarBtn" Grid.Row="0" Grid.ColumnSpan="2"/>

                <CheckBox Margin="0,0,20,0" HorizontalAlignment="Right" VerticalAlignment="Center" Content="자동 조회" IsChecked="True" x:Name="ckSearch" Foreground="White" 
                Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked">
                    <CheckBox.Resources>
                        <Style TargetType="Path">
                            <Setter Property="Fill" Value="Red"/>
                        </Style>
                    </CheckBox.Resources>
                </CheckBox>

                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="2" Margin="5" Cursor="">

                    <Label Content="차종" Style="{StaticResource TopLabelStyle}"/>
                    <ComboBox x:Name="cbCarCode" Style="{StaticResource ComboBoxStyle}" Margin="0,5,0,5" DisplayMemberPath="CODE_NAME" SelectedIndex="0"  Width="120"/>

                    <Label Content="ALC 코드" Style="{StaticResource TopLabelStyle}"/>
                    <Border Grid.Row="1" Margin="5,5,5,5" BorderBrush="LightGray" BorderThickness="1" CornerRadius="2" Width="120">
                        <TextBox x:Name="txtAlcCode" Text="" Style="{StaticResource textBoxStyle}" BorderBrush="Transparent" VerticalAlignment="Center" Margin="5" />
                    </Border>

                    <Label Content="BODY NO" Style="{StaticResource TopLabelStyle}"/>
                    <Border Grid.Row="1" Margin="5,5,5,5" BorderBrush="LightGray" BorderThickness="1" CornerRadius="2" Width="120">
                        <TextBox x:Name="txtBodyNo" Text="" Style="{StaticResource textBoxStyle}" BorderBrush="Transparent" VerticalAlignment="Center" Margin="5" />
                    </Border>

                    <Label Content="COMMIT NO" Style="{StaticResource TopLabelStyle}"/>
                    <Border Grid.Row="1" Margin="5,5,5,5" BorderBrush="LightGray" BorderThickness="1" CornerRadius="2" Width="120">
                        <TextBox x:Name="txtCommitNo" Text="" Style="{StaticResource textBoxStyle}" BorderBrush="Transparent" VerticalAlignment="Center" Margin="5" />
                    </Border>

                    <Label Content="상태" Style="{StaticResource TopLabelStyle}" Width="50"/>
                    <ComboBox x:Name="cbStatus" Style="{StaticResource ComboBoxStyle}" Margin="0,5,0,5" SelectedIndex="0"  Width="120"/>

                    <Label Content="재고 상태" Style="{StaticResource TopLabelStyle}" Width="80"/>
                    <ComboBox x:Name="cbStock" Style="{StaticResource ComboBoxStyle}" Margin="0,5,0,5" SelectedIndex="0"  Width="120"/>

                    <Button x:Name="btnOut" Style="{StaticResource LoginButton}" Width="100" Margin="20,10,5,10" Click="btnOut_Click" >
                        <StackPanel Orientation="Horizontal" Margin="10 0">
                            <Icon:PackIconMaterial Kind="LocationExit"  VerticalAlignment="Center"/>
                            <TextBlock Text=" 출고" FontWeight="Bold" />
                        </StackPanel>
                    </Button>


                    <Border Grid.Row="1" Margin="30,5,5,5" BorderBrush="LightGray" BorderThickness="1" CornerRadius="2" Width="80"  Visibility="Visible">
                        <TextBox x:Name="txtSkidCnt" Text="16" Style="{StaticResource textBoxStyle}" BorderBrush="Transparent" VerticalAlignment="Center" Margin="5" PreviewTextInput="txtSkidCnt_PreviewTextInput" PreviewKeyDown="txtSkidCnt_PreviewKeyDown" />
                    </Border>

                    <Button x:Name="btnCntOut" Style="{StaticResource CancelButton}" Width="140" Margin="5,10,5,10" Click="btnCntOut_Click" Visibility="Visible">
                        <StackPanel Orientation="Horizontal" Margin="10 0">
                            <Icon:PackIconMaterial Kind="LocationExit"  VerticalAlignment="Center"/>
                            <TextBlock Text=" 지정 갯수 출고" FontWeight="Bold" />
                        </StackPanel>
                    </Button>

                </StackPanel>

                <DataGrid x:Name="HostDataGrid" Grid.Row="2" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" >
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="서열 삭제" Click="hostDel_Click"/>
                            <MenuItem Header="ALC 변경" Click="hostUpdate_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>

                        <DataGridTextColumn Header="NO" IsReadOnly="True" Width="90" Binding="{Binding ROW_NUM}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="의장투입일시" IsReadOnly="True" Width="150" Binding="{Binding REPORT_SYS_DT}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="COMMIT_NO" IsReadOnly="True" Width="100" Binding="{Binding CMT_NO}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="의장ALC" IsReadOnly="True" Width="100" Binding="{Binding ALC_CODE}" ElementStyle="{StaticResource BoldCellStyle}"/>
                        <DataGridTextColumn Header="BODY_NO" IsReadOnly="True" Width="130" Binding="{Binding BODY_NO}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="지역" IsReadOnly="True" Width="130" Binding="{Binding HANGUEL_PART}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="사양국" IsReadOnly="True" Width="130" Binding="{Binding REGION_NAME}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="내장색" IsReadOnly="True" Width="70" Binding="{Binding INNER_COLOR}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="차종" IsReadOnly="True" Width="70" Binding="{Binding ALC_CLASS}" ElementStyle="{StaticResource BoldCellStyle}"/>
                        <DataGridTextColumn Header="FR" IsReadOnly="True" Width="70" Binding="{Binding STOCK_FR}" ElementStyle="{StaticResource RedTextIfLackStyle}" />
                        <DataGridTextColumn Header="RR" IsReadOnly="True" Width="70" Binding="{Binding STOCK_R2}" ElementStyle="{StaticResource RedTextIfLackStyleR2}"/>
                        <DataGridTextColumn Header="작업상태" IsReadOnly="True" Width="70" Binding="{Binding STATUS}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                        <DataGridTextColumn Header="수신일자" IsReadOnly="True" Width="210" Binding="{Binding CR_DATE}" ElementStyle="{StaticResource DataGridCellCenter}"/>

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
